{
    "name": "chess-challenge",
    "private": true,
    "scripts": {
        "start": "concurrently --no-color --kill-others-on-fail --names server,client \"npm run start --prefix server\" \"npm run preview --prefix client -- --host\""
    },
    "dependencies": {
        "concurrently": "^9.2.0",
        "vite": "^8.3.0"
    }
}
