# Chess Challenge for Iyad.

Run the client :

```bash
cd client
npm install
npm run dev
```

Run the server :

```bash
cd server
npm install
npm run dev
```

or build the docker image and run it :

```bash
docker build -t iyad-chess .
# Then
docker run -p 3000:3000 iyad-chess
```
